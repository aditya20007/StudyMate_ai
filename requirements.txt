# ============================================================
# StudyMate AI — Requirements (Render Free Tier, No Local ML)
# 
# sentence-transformers and torch REMOVED — uses Groq API instead.
# This keeps total RAM under 512MB (Render free tier limit).
# ============================================================

# --- Web Framework ---
fastapi
uvicorn[standard]
python-multipart

# --- Database ---
sqlalchemy
aiosqlite

# --- AI / LLM + Embeddings (Groq — free API, no local model) ---
openai

# --- Vector Store (CPU only, no GPU packages) ---
faiss-cpu
numpy

# --- PDF Processing ---
pdfplumber
PyMuPDF

# --- YouTube ---
youtube-transcript-api
yt-dlp

# --- Speech / Audio ---
gTTS

# --- Text Processing ---
tiktoken

# --- Utilities ---
python-dotenv
pydantic
pydantic-settings
httpx
aiofiles
requests
tenacity
loguru
rich
tqdm